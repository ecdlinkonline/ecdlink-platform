"use client";

import Link from "next/link";
import { useRouter } from "next/navigation";
import { CalendarClock, CheckCircle2, ClipboardList, FileClock, FileText, HandCoins, PlusCircle } from "lucide-react";
import { DataTable, KpiCard, PageHeader, StatusBadge } from "@/components/design-system";
import { WorkflowActionDialog, type WorkflowActionResult } from "@/components/workflows/workflow-action-dialog";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { formatGrantCurrency, formatGrantLabel, reportTypeLabels } from "@/lib/grant-reports/types";
import { grantReportEmptyStateMessage } from "@/lib/grant-reports/presentation";
import type { GrantReportWorkspaceData } from "@/lib/grant-reports/types";
import type { GrantReportFiltersInput } from "@/lib/validators/grant-reports";

const tabs = [{ id: "reports", label: "Reports" }, { id: "awards", label: "Grant Awards" }, { id: "obligations", label: "Reporting Obligations" }] as const;
type TabId = typeof tabs[number]["id"];

type AwardValues = { sourceType: string; fundingApplicationId: string; sponsorshipCommitmentId: string; centreId: string; fundingProjectId: string; awardNumber: string; title: string; description: string; awardedAmount: number; currency: string; startDate: string; endDate: string; organisationType: string; fundingOrganisationId: string; donorOrganisationId: string; organisationRole: string; canReview: boolean; canApprove: boolean };
type ObligationValues = { grantAwardId: string; grantTrancheId: string; type: string; basis: string; title: string; description: string; reportingPeriodStart: string; reportingPeriodEnd: string; financialYear: string; quarter: number; dueAt: string; requiresFunderApproval: boolean; requiresSuperAdminApproval: boolean };

async function postAction(url: string, values: Record<string, string | number | boolean>): Promise<WorkflowActionResult> {
  const response = await fetch(url, { method: "POST", headers: { "Content-Type": "application/json" }, body: JSON.stringify(values) });
  const body = await response.json().catch(() => null) as { ok?: boolean; error?: string; details?: { fieldErrors?: Record<string, string[]> } } | null;
  if (!response.ok) return { ok: false, error: body?.error ?? "The action could not be completed.", fieldErrors: Object.fromEntries(Object.entries(body?.details?.fieldErrors ?? {}).map(([name, messages]) => [name, messages[0] ?? "Invalid value."])) };
  return { ok: true };
}

function dateLabel(value: string | null) {
  return value ? new Intl.DateTimeFormat("en-ZA", { dateStyle: "medium" }).format(new Date(value)) : "—";
}

export function GrantReportsWorkspace({ data, initialTab, filters }: { data: GrantReportWorkspaceData; initialTab?: string; filters: GrantReportFiltersInput }) {
  const router = useRouter();
  const activeTab: TabId = tabs.some((tab) => tab.id === initialTab) ? initialTab as TabId : "reports";
  const refresh = () => router.refresh();
  const organisationOptions = [
    ...data.options.fundingOrganisations.map((organisation) => ({ label: `Funding · ${organisation.name}`, value: organisation.id })),
    ...data.options.donorOrganisations.map((organisation) => ({ label: `Donor · ${organisation.name}`, value: organisation.id })),
  ];

  return <div className="space-y-6">
    <PageHeader eyebrow="Super Admin" title="Grant Reports" description="Oversee confirmed grant awards, reporting obligations and formal report lifecycles across ECDLink." actions={<><AwardDialog data={data} onSuccess={refresh} /><ObligationDialog data={data} onSuccess={refresh} /><Link href="/dashboard/super-admin"><Button variant="secondary">Back to dashboard</Button></Link></>} />
    <div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-6">
      <KpiCard label="Active Awards" value={String(data.metrics.activeAwards)} description="Confirmed active grants" icon={HandCoins} />
      <KpiCard label="Reports Due" value={String(data.metrics.reportsDue)} description="Open and overdue" icon={CalendarClock} tone="warning" />
      <KpiCard label="Draft Reports" value={String(data.metrics.draftReports)} description="Awaiting preparation" icon={FileClock} />
      <KpiCard label="Submitted" value={String(data.metrics.submittedReports)} description="Under review" icon={ClipboardList} />
      <KpiCard label="Returned" value={String(data.metrics.returnedReports)} description="Requires revision" icon={FileText} tone="warning" />
      <KpiCard label="Approved" value={String(data.metrics.approvedReports)} description="Completed review" icon={CheckCircle2} tone="green" />
    </div>
    <div className="flex gap-2 overflow-x-auto rounded-lg border border-brand-line bg-white p-2 dark:border-slate-800 dark:bg-slate-900">{tabs.map((tab) => <Link key={tab.id} href={`?tab=${tab.id}`} className={`shrink-0 rounded-lg px-4 py-2 text-sm font-bold ${activeTab === tab.id ? "bg-brand-navy text-white" : "text-slate-600 hover:bg-brand-accent dark:text-slate-300"}`}>{tab.label}</Link>)}</div>
    {activeTab === "reports" ? <ReportsSection data={data} filters={filters} organisations={organisationOptions} /> : null}
    {activeTab === "awards" ? <AwardsSection data={data} /> : null}
    {activeTab === "obligations" ? <ObligationsSection data={data} /> : null}
  </div>;
}

function ReportsSection({ data, filters, organisations }: { data: GrantReportWorkspaceData; filters: GrantReportFiltersInput; organisations: Array<{ label: string; value: string }> }) {
  return <Card className="dark:border-slate-800 dark:bg-slate-900"><CardHeader><CardTitle className="dark:text-white">Reports register</CardTitle><CardDescription className="dark:text-slate-400">Search and filter persisted grant reports. Full editing begins in Phase 2.</CardDescription></CardHeader><CardContent className="space-y-4">
    <form className="grid gap-3 md:grid-cols-2 xl:grid-cols-5"><input name="query" defaultValue={filters.query} placeholder="Search reports" className="rounded-lg border border-brand-line px-3 py-2 text-sm" /><select name="status" defaultValue={filters.status ?? ""} className="rounded-lg border border-brand-line px-3 py-2 text-sm"><option value="">All statuses</option>{["DRAFT","SUBMITTED","RETURNED","APPROVED","ARCHIVED"].map((status)=><option key={status}>{status}</option>)}</select><select name="type" defaultValue={filters.type ?? ""} className="rounded-lg border border-brand-line px-3 py-2 text-sm"><option value="">All types</option>{Object.entries(reportTypeLabels).map(([value,label])=><option key={value} value={value}>{label}</option>)}</select><select name="centreId" defaultValue={filters.centreId ?? ""} className="rounded-lg border border-brand-line px-3 py-2 text-sm"><option value="">All centres</option>{data.options.centres.map((centre)=><option key={centre.id} value={centre.id}>{centre.centreName}</option>)}</select><select name="organisationId" defaultValue={filters.organisationId ?? ""} className="rounded-lg border border-brand-line px-3 py-2 text-sm"><option value="">All funders</option>{organisations.map((option)=><option key={`${option.label}-${option.value}`} value={option.value}>{option.label}</option>)}</select><input type="hidden" name="tab" value="reports" /><div className="flex gap-2"><Button type="submit">Apply filters</Button><Link href="?tab=reports"><Button type="button" variant="ghost">Clear</Button></Link></div></form>
    {data.reports.length ? <DataTable columns={["Report","Centre","Funder / Partner","Project","Type","Reporting Period","Due","Status","Version","Actions"]} rows={data.reports.map((report)=>[<span key="title" className="font-bold">{report.title}</span>,report.centre,report.funder,report.project,reportTypeLabels[report.type],`${dateLabel(report.reportingPeriodStart)} – ${dateLabel(report.reportingPeriodEnd)}`,dateLabel(report.dueAt),<StatusBadge key="status" status={formatGrantLabel(report.status)} />,`v${report.version}`,<span key="action" className="text-xs font-semibold text-slate-500">Editor in Phase 2</span>])} /> : <div className="rounded-lg border border-dashed border-brand-line p-8 text-center"><FileText className="mx-auto h-8 w-8 text-slate-400" /><p className="mt-3 font-bold">No reports found</p><p className="mt-2 text-sm text-slate-500">{grantReportEmptyStateMessage()}</p></div>}
  </CardContent></Card>;
}

function AwardsSection({ data }: { data: GrantReportWorkspaceData }) { return <Card className="dark:border-slate-800 dark:bg-slate-900"><CardHeader><CardTitle className="dark:text-white">Grant Awards</CardTitle><CardDescription>Confirmed funding relationships. Awards are never created automatically.</CardDescription></CardHeader><CardContent>{data.awards.length ? <DataTable columns={["Award Number","Centre","Project","Lead Funder / Partner","Awarded","Dates","Status","Tranches","Obligations","Actions"]} rows={data.awards.map((award)=>[<span key="number" className="font-bold">{award.awardNumber}</span>,award.centre,award.project,award.leadOrganisation,formatGrantCurrency(award.awardedAmount,award.currency),`${dateLabel(award.startDate)} – ${dateLabel(award.endDate)}`,<StatusBadge key="status" status={formatGrantLabel(award.status)} />,String(award.trancheCount),String(award.obligationCount),<span key="action" className="text-xs font-semibold text-slate-500">Managed here</span>])} /> : <p className="rounded-lg border border-dashed p-8 text-center text-sm text-slate-500">No confirmed Grant Awards yet. Use “Create Grant Award” to explicitly convert an approved relationship or record a manual agreement.</p>}</CardContent></Card>; }

function ObligationsSection({ data }: { data: GrantReportWorkspaceData }) { return <Card className="dark:border-slate-800 dark:bg-slate-900"><CardHeader><CardTitle className="dark:text-white">Reporting Obligations</CardTitle><CardDescription>Each obligation creates one Draft report and version 1 for future preparation.</CardDescription></CardHeader><CardContent>{data.obligations.length ? <DataTable columns={["Obligation","Award","Centre","Funder","Type","Basis","Tranche","Due","Status","Report"]} rows={data.obligations.map((item)=>[<span key="title" className="font-bold">{item.title}</span>,item.awardNumber,item.centre,item.funder,reportTypeLabels[item.type],formatGrantLabel(item.basis),item.tranche ? `Tranche ${item.tranche.trancheNumber}` : "—",dateLabel(item.dueAt),<StatusBadge key="status" status={formatGrantLabel(item.status)} />,item.report ? `${formatGrantLabel(item.report.status)} · v${item.report.currentVersionNumber}` : "Not created"])} /> : <p className="rounded-lg border border-dashed p-8 text-center text-sm text-slate-500">No reporting obligations yet. Create an award first, then add its required reporting schedule.</p>}</CardContent></Card>; }

function AwardDialog({ data, onSuccess }: { data: GrantReportWorkspaceData; onSuccess: () => void }) {
  return <WorkflowActionDialog<AwardValues> title="Create Grant Award" description="Explicitly confirm an approved funding relationship. Server validation will verify every source, centre, project and organisation relationship." size="xl" trigger={{ label: "Create Grant Award", icon: <PlusCircle className="h-4 w-4" /> }} confirmationButton={{ label: "Confirm Award", loadingLabel: "Creating…" }} initialValues={{ sourceType:"MANUAL",fundingApplicationId:"",sponsorshipCommitmentId:"",centreId:"",fundingProjectId:"",awardNumber:"",title:"",description:"",awardedAmount:0,currency:"ZAR",startDate:"",endDate:"",organisationType:"FUNDING_ORGANISATION",fundingOrganisationId:"",donorOrganisationId:"",organisationRole:"LEAD_FUNDER",canReview:true,canApprove:false }} fields={[{name:"sourceType",label:"Source Type",type:"select",required:true,options:[{label:"Manual confirmation",value:"MANUAL"},{label:"Approved Funding Application",value:"FUNDING_APPLICATION"},{label:"Sponsorship Commitment",value:"SPONSORSHIP_COMMITMENT"}]},{name:"fundingApplicationId",label:"Source Funding Application",type:"select",options:data.options.applications.map((item)=>({label:item.label,value:item.id}))},{name:"sponsorshipCommitmentId",label:"Source Sponsorship Commitment",type:"select",options:data.options.commitments.map((item)=>({label:item.label,value:item.id}))},{name:"centreId",label:"Centre",type:"select",required:true,options:data.options.centres.map((item)=>({label:item.centreName,value:item.id}))},{name:"fundingProjectId",label:"Funding Project",type:"select",required:true,options:data.options.projects.map((item)=>({label:`${item.centreName} · ${item.title}`,value:item.id}))},{name:"awardNumber",label:"Award Number",type:"text",required:true,maxLength:100},{name:"title",label:"Title",type:"text",required:true,maxLength:200},{name:"description",label:"Description",type:"textarea",maxLength:5000},{name:"awardedAmount",label:"Awarded Amount",type:"number",required:true,min:0,step:0.01},{name:"currency",label:"Currency",type:"text",required:true,minLength:3,maxLength:3},{name:"startDate",label:"Start Date",description:"Use YYYY-MM-DD",type:"text",required:true},{name:"endDate",label:"End Date",description:"Optional; use YYYY-MM-DD",type:"text"},{name:"organisationType",label:"Lead Organisation Type",type:"select",required:true,options:[{label:"Funding organisation",value:"FUNDING_ORGANISATION"},{label:"Donor organisation",value:"DONOR_ORGANISATION"}]},{name:"fundingOrganisationId",label:"Funding Organisation",type:"select",options:data.options.fundingOrganisations.map((item)=>({label:item.name,value:item.id}))},{name:"donorOrganisationId",label:"Donor Organisation",type:"select",options:data.options.donorOrganisations.map((item)=>({label:item.name,value:item.id}))},{name:"organisationRole",label:"Organisation Role",type:"select",required:true,options:["LEAD_FUNDER","CO_FUNDER","IMPLEMENTATION_PARTNER","OVERSIGHT_PARTNER"].map((value)=>({label:formatGrantLabel(value),value}))},{name:"canReview",label:"Organisation may review reports",type:"checkbox"},{name:"canApprove",label:"Organisation may approve reports",type:"checkbox"}]} action={(values)=>postAction("/api/grant-awards",values)} successToast={{title:"Grant Award created"}} errorToast={{title:"Award creation failed",fallbackDescription:"The award could not be created."}} onSuccess={onSuccess} />;
}

function ObligationDialog({ data, onSuccess }: { data: GrantReportWorkspaceData; onSuccess: () => void }) {
  const trancheOptions=data.options.awards.flatMap((award)=>award.tranches.map((tranche)=>({label:tranche.label,value:tranche.id})));
  return <WorkflowActionDialog<ObligationValues> title="Create Reporting Obligation" description="Creates the obligation, one Draft report and version 1 in a single transaction. Submission snapshots are not created yet." size="lg" trigger={{label:"Add Obligation",variant:"secondary",icon:<PlusCircle className="h-4 w-4"/>}} confirmationButton={{label:"Create Obligation",loadingLabel:"Creating…"}} initialValues={{grantAwardId:"",grantTrancheId:"",type:"INTERIM",basis:"PERIOD",title:"",description:"",reportingPeriodStart:"",reportingPeriodEnd:"",financialYear:"",quarter:1,dueAt:"",requiresFunderApproval:true,requiresSuperAdminApproval:false}} fields={[{name:"grantAwardId",label:"Grant Award",type:"select",required:true,options:data.options.awards.map((item)=>({label:item.label,value:item.id}))},{name:"type",label:"Report Type",type:"select",required:true,options:Object.entries(reportTypeLabels).map(([value,label])=>({label,value}))},{name:"basis",label:"Obligation Basis",type:"select",required:true,options:["QUARTER","TRANCHE","PERIOD","FINAL","CUSTOM"].map((value)=>({label:formatGrantLabel(value),value}))},{name:"grantTrancheId",label:"Tranche",type:"select",options:trancheOptions},{name:"title",label:"Title",type:"text",required:true,maxLength:200},{name:"description",label:"Description",type:"textarea",maxLength:5000},{name:"reportingPeriodStart",label:"Reporting Period Start",description:"Use YYYY-MM-DD",type:"text"},{name:"reportingPeriodEnd",label:"Reporting Period End",description:"Use YYYY-MM-DD",type:"text"},{name:"financialYear",label:"Financial Year",type:"text",maxLength:20},{name:"quarter",label:"Quarter",type:"number",min:1,max:4},{name:"dueAt",label:"Due Date",description:"Use YYYY-MM-DD",type:"text",required:true},{name:"requiresFunderApproval",label:"Requires funder approval",type:"checkbox"},{name:"requiresSuperAdminApproval",label:"Requires Super Admin approval",type:"checkbox"}]} action={(values)=>postAction("/api/grant-reporting-obligations",values)} successToast={{title:"Reporting obligation created",description:"Draft report version 1 is ready for Phase 2 preparation."}} errorToast={{title:"Obligation creation failed",fallbackDescription:"The reporting obligation could not be created."}} onSuccess={onSuccess}/>;
}
